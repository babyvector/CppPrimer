* 15.1-Q:什么是虚成员？
* A:在C++中虚成员指的就是虚函数；这个语言中借用虚函数来实现运行时的多态。
* web-A:基类中的虚拟成员希望其派生类定义其自己的版本。特别是，基类通常应定义一个虚拟析构函数，即使它不起作用。
* 15.2-Q：protected访问说明符与private有何区别？
* A：protected表示只有自己和自己的派生类有权访问该成员，同时禁止其他用户访问。private只有自己和自己的友员能够访问。
* web-A:私有成员：基类和朋友可以访问；受保护的成员：基类本身，朋友和派生类都可以访问